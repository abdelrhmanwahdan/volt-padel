node_modules
.next
out
.env
.env*.local
.vercel
.wrangler
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Top-level source media folders (large raw assets — kept locally, never committed).
# Leading "/" pins to repo root so we don't accidentally match public/videos etc.
/frames/
/videos/
/images/
/logs/

# Asset enhancement (Real-ESRGAN) artifacts — never commit
/public.bak/
/.upscale.log
/.upscale_work/
